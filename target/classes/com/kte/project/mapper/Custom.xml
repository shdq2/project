<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="custom">
	<!-- <select id="selectBoardList" resultType="com.kte.mvc.vo.V6_Board">
		SELECT BRD_NO, BRD_TITLE, BRD_WRITER, BRD_HIT, TO_CHAR(BRD_HIT, '999,999,999,999') AS BRD_HIT1, 
		TO_CHAR(BRD_DATE, 'YYYY-MM-DD') AS BRD_DATE FROM V6_BOARD ORDER BY BRD_NO DESC
	</select>
	
	<select id="selectBoardOne" resultType="com.kte.mvc.vo.V6_Board">
		SELECT BRD_NO, BRD_TITLE, BRD_WRITER, BRD_HIT, TO_CHAR(BRD_HIT, '999,999,999,999') AS BRD_HIT1, 
		BRD_DATE FROM V6_BOARD WHERE BRD_NO = #{brd_no}
	</select>
	
	<select id="selectLastNo" resultType="int">
		SELECT NVL(MAX(BRD_NO),0) FROM V6_BOARD
	</select>
	
	<insert id="insertBoard" parameterType="com.kte.mvc.vo.V6_Board">
		INSERT INTO V6_BOARD(BRD_NO, BRD_TITLE, BRD_CONTENT, BRD_HIT, BRD_WRITER, BRD_DATE) VALUES(#{brd_no},#{brd_title},#{brd_content},#{brd_hit},#{brd_writer},SYSDATE)
	</insert>
	
	<update id="updateBoardHit" parameterType="int">
		UPDATE V6_BOARD SET BRD_HIT = BRD_HIT + 1 WHERE BRD_NO = #{brd_no}
	</update> -->
	
	<select id="selectCustomOne" parameterType="com.kte.project.VO.CustomVO" resultType="com.kte.project.VO.CustomVO">
		SELECT 
		custom_name,bank_code,custom_cash,custom_phone,date_format(custom_date,'%Y년 %m월 %d일 %h시 %i분') as custom_date,custom_gender,
		custom_bank_host,custom_age,custom_introduce,custom_homepage,custom_id,custom_job
		 FROM custom WHERE custom_id = #{custom_id}
	</select>
	
	<update id="passwordEdit" parameterType="String">
		UPDATE custom SET custom_pw = SHA1(#{pw})
	</update>
	
	<update id="custom_edit" parameterType="customVO">
		update custom set custom_name=#{custom_name},bank_code=#{bank_code},custom_cash=#{custom_cash},custom_phone=#{custom_phone},custom_gender=#{custom_gender},custom_bank_host=#{custom_bank_host},
		custom_job=#{custom_job},custom_age=#{custom_age},custom_homepage=#{custom_homepage},custom_introduce=#{custom_introduce}
		where custom_id=#{custom_id}
	</update>
	
	<!-- 은행 목록 불러오기 -->
	<select id="bank_list" resultType="customVO">
		select * from bank
	</select>
	
	<update id="delete_phone" parameterType="String">
		update custom set custom_phone = '' where custom_id=#{id}
	</update>
</mapper>