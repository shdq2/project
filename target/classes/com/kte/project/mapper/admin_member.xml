<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin_member">
		
	<select id="AdminUserMain" resultType="customVO" parameterType="Int">
		select c.*,b.bank_name from bank b 
		inner join (select * from custom where custom_block <![CDATA[<]]> 10 order by custom_date desc) c
		on b.bank_code = c.bank_code order by custom_date desc		
		limit #{page},10
	</select>

	<select id="room_count" resultType="Int" parameterType="String">
		select count(room_code) as room_count from room where custom_id = #{custom_id}
	</select>
	
	<update id="member_block" parameterType="customVO">
		update custom set custom_block = #{custom_block} where custom_id=#{custom_id}
	</update>
	
	<select id="admin_member_select" parameterType="String" resultType="customVO">
		select * from custom where custom_id=#{id}
	</select>
	<select id="admin_hope_list" resultType="roomVO" parameterType="hashmap">
		select r.*,c.custom_name from custom c 
		inner join
		(select r.*,a.room_addr from room_addr a
			inner join
			(select room_code,room_name,custom_id,room_day,room_min_day,room_max_day from room where room_code in 
				<foreach collection="array" item="item" index="index" separator="," open="(" close=")">
					#{item}
				</foreach>) r
			on a.room_code = r.room_code) r
		on c.custom_id = r.custom_id		
	</select>
</mapper>
